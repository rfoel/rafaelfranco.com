const darkGray = '#4b4b4b'
const green = '#068757'
const white = '#ffffff'

const colors = {
  accent: green,
  background: white,
  foreground: darkGray,
}

const theme = {
  colors,
}

export default theme
