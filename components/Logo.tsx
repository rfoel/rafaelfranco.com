import Typed from 'react-typed'

const Logo = () => (
  <Typed
    backDelay={1000}
    backSpeed={50}
    showCursor={false}
    strings={['Rafael Franco', 'rfoel.dev']}
    typeSpeed={50}
    loop
  />
)

export default Logo
