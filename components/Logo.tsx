import Typed from 'react-typed'

const Logo = () => (
  <Typed
    backDelay={1000}
    backSpeed={50}
    loop
    showCursor={false}
    strings={['Rafael Franco', 'rfoel.dev']}
    typeSpeed={50}
  />
)

export default Logo
